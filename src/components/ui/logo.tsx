import Link from 'next/link';

export function Logo({ className = "", size = "medium" }: { className?: string, size?: "small" | "medium" | "large" }) {
  // No mostrar nada cuando se llame a este componente
  return null;
} 